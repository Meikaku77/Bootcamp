import { StyleSheet } from "react-native"

export const GlobalStyles = StyleSheet.create({
    centerContainer: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    },
    title: {
        fontSize: 45,
        fontWeight: '300',
        color: 'black'
    }
})