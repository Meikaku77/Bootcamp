/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {ComponentsApp} from './src/MasComponentes';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => ComponentsApp);
