export interface UpcomingMovie{
    backdropPath: string
    genreIds: number[]
    id: number
    originalLanguage: string
    originalTitle: string
    posterPath: string,
    releaseDate: Date
    rate: number
}